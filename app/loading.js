import React from "react";

const Loading = () => {
  return (
    <div className="fixed flex items-center justify-center text-4xl font-semibold text-white top-0 left-0 w-full h-full bg-indigo-400 ">
      loading .. .. ..
    </div>
  );
};

export default Loading;
